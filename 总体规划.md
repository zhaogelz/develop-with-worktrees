# 总体规划

## 产品定位

`develop-with-worktrees` 是技术栈无关的 Codex 本地 Git 生命周期内核。它解决“一个人或小团队在多个 AI 任务间安全并行”的共同问题，不接管仓库的部署、数据库、认证或 PR 流程。

## 固定边界

- 任务从调用位置当前本地分支派生，Finish 回到记录的同一基线工作树；不固定 `main`。
- 只做本地 commit、合并与恢复；推送、PR 和远端协作由用户处理。
- 仓库只声明验证路径、命令、层级、资源类别及手动清理边界；机器性能和队列容量保持本地。
- 不引入 `workspace.toml`。复杂仓库仍先用当前两个策略文件；真正需要时再单独设计适配层。

## 最终交付形态

```text
当前分支 Start
  → 隔离工作树与精确提交
  → 已登记验证 + 本机共享队列
  → Ready 精确证明
  → 本地 FIFO Finish 到记录基线
```

清理永远与交付分离：Finish 保留依赖和缓存；只有用户手动确认、且命中精确所有权声明的 `prune-slot` 才可删除。
