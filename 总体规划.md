# 总体规划

## 产品定位

`develop-with-worktrees` 是一个本地优先的多 AI 开发指挥中心。用户只需在中央对话描述目标；简单任务仍由一个 AI 直接完成，复杂任务在一次可读计划确认后拆成可独立验收的工作，由最多 5 个 AI 协作推进。底层 DWW 生命周期继续负责隔离目录、精确提交、验证和只在本地的有序合入。

## 三层结构

```text
中央对话 / 规划 AI：理解自然语言、给出一次任务计划、汇报例外与结果
        ↓
编排层：任务图、前沿调度、最少状态、暂停恢复、验收账本、适配器边界
        ↓
DWW / 仓库生命周期：隔离工作树、Commit、Ready、Finish、本地验证与合入
```

## 固定原则

- 用户不需要了解分支、工作树、技能或调度器；默认只看到计划确认、必要的业务问题和最终结果。
- 简单任务不启动多 AI。复杂任务先给通俗的纵向任务计划，用户确认一次后才派发；未确认前不启动 worker。
- 开发并发硬上限是 5，实际并发取 `min(5, 仓库已配置槽位, 当前空闲槽位)`；验证继续复用现有加权队列，不设固定并行数。
- 每个任务只有一名写入者。总控会话是唯一派发入口；worker 不得再派发 worker。默认不创建审查 AI、不启动常驻后台。
- 普通任务采用乐观并行：同一文件本身不串行；Git 可干净合并且验证通过时自动仅在本地合入。迁移、锁文件、共享契约等显式高风险资源才独占。
- Ready 在取得机器验证准入后核对记录的基线；并发任务推进主线时，同一次 Ready 自动同步并收敛，复用输入未变的内容证明。持续推进超过有界次数时保留候选并明确失败，Finish 继续承担最终一致性和本地合入。
- Finish、Recover 与 Abandon 共享本地集成串行边界。Finish 在提升前持久化不可变候选事务，Recover 以 Git 祖先关系作为合入事实，分支只按预期 SHA 原子删除；进程中断、分支漂移或工作树新增内容都不会被强制清理。
- 槽位依赖清理始终是手工、一次性的计划操作，绑定槽位世代，并把敏感数据、数据库、上传/存储目录和链接视为不可删除内容。
- 文本或语义冲突不猜测：由原任务 owner 从最新基线领取新的修复任务；跨任务问题创建独立集成修复任务，绝不重开旧完成分支。
- 阻塞只暂停其下游；调度器继续派发无关前沿。两次无代码变化的相同失败停止并上报；有进展的修复受可配置预算约束。
- 编排状态只保存在 Git common-dir 的 `solo-ai-orchestration` 独立目录，记录目标、任务、依赖、状态、决策摘要、生命周期/证明引用；不保存聊天记录、原始推理、lease 或密钥。完成后另写紧凑回执。
- 暂停、取消和会话中断都保留任务分支与文件；新中央会话接手后可继续。删除或放弃仍需单独显式确认。
- 只自动完成本地 Finish，绝不自动 push、创建 PR、部署或发布。
- 普通仓库使用 DWW 生命周期适配器；成熟仓库仅能通过明确的委托适配器接入，且已有外部编排器时整体让路。绝不解析任意自然语言来猜外部命令。
