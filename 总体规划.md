# Develop with Worktrees 总体规划

## 1. 产品方向

`develop-with-worktrees` 是面向 Codex 的本地优先 Git worktree 生命周期插件。它的目标是让个人项目、团队 PR 分支和复杂单仓都能安全地并行开发，而不把任一业务仓库的数据库、部署或认证规则硬编码进插件。

唯一架构为：技术栈无关的生命周期内核，加上可选的声明式仓库适配器。

## 2. 分层边界

### 生命周期内核

- 采用、停用、让位与安全卸载。
- 任务槽位、租约、精确提交、队列、恢复与本地集成。
- 基于 Start 时记录的本地基线分支创建任务；不再假定基线总是 `main`。
- 明确 argv、受控验证环境、进程身份、超时、日志与证明。
- 只删除具有明确所有权且经再次核验的本地内容。

### 声明式仓库适配

- `.solo-ai/config.toml`：生命周期、槽位和安全策略。
- `.solo-ai/verification.toml`：验证范围、输入闭包、执行层级与资源限制。
- `.solo-ai/workspace.toml`：复杂单仓可选声明，描述项目单元、依赖、服务、运行命名空间和清理所有权。
- 自动发现只生成待审核草案；扫描阶段只读，绝不自动执行仓库代码。

## 3. 交付模型

- 个人项目可将已验证任务以 `ff-only` 合回记录的本地基线分支。
- 团队项目可将多个任务合回本地 PR 基线分支后，由用户自行推送和创建 PR。
- 后续支持保留已验证任务分支与重新打开任务；插件不自动 fetch、pull、push 或创建 PR。
- 可选远端检查只允许显式配置的 fetch，默认关闭。

## 4. 验证模型

```text
开发期已登记 profile 验证
        ↓
可复用 profile 证据
        ↓
Ready：同步记录的基线、计算必要闭包、只重跑受影响 profile
        ↓
精确候选证明
        ↓
Finish：复核并交付
```

- 日常开发默认执行 Ready 必要闭包。
- 全量验证只能显式触发，用于里程碑、部署、发布或用户要求。
- 任意自定义命令只能提供开发反馈，不能成为 Finish 证明。
- 未映射活跃路径、未知外部状态、输入闭包不完整或日志缺失均不能被静默跳过。
- 本机保存 profile 耗时历史，只用于预计耗时与拆分/优化提醒，不上传、不提交。

## 5. 安全与可恢复性

- 工作树隔离不宣称等于操作系统安全沙箱。
- 验证进程只得到基础运行环境和 profile 显式声明的变量；原值不写入日志或状态。
- 每次长操作均有持久化小票和验证运行记录；调用者中断时可诊断真实子进程，禁止盲目恢复或强杀未知进程。
- 不自动清理依赖、缓存、未知忽略文件、`.env*`、数据库或业务数据。
- 清理、放弃、裁剪和卸载必须以精确计划、所有权证明和显式确认执行。

## 6. 演进原则

- 新能力优先用于新任务；在途任务保持安全完成和退出路径，可在干净状态下显式升级并重新 Ready。
- 普通仓库保持低配置；复杂能力按需显现。
- 不整体重写。先以测试固定行为，再分模块替换实现。
- `亿发姆 x` 当前仅作为复杂仓库能力基准，不在本阶段迁移或接管其成熟流程。
