# 总体规划

## 产品定位

`develop-with-worktrees` 是技术栈无关的本地 Git 生命周期内核，用于让个人或小团队同时推进多个 AI 修改任务，而不让任务互相污染。核心 CLI 可被不同宿主适配；当前插件为 Codex 提供技能、钩子和默认工作方式。

## 固定原则

- 默认把修改放入独立工作树；用户不需要手动维护工作树。
- 基线来自调用位置的当前本地分支，不固定 `main`；Finish 只做本地合并，推送和 PR 仍由团队决定。
- 只有用户明确需要当前工作树状态时，才创建一次会话绑定的 `in-place` 任务。它不建槽位或分支，身份受工作树、分支、HEAD、会话和租约共同约束。
- 直改的验证基线是不可变的开始提交；Finish 不合并、切分支、reset、clean 或删除缓存。异常只保留和隔离现场。
- 仓库只声明验证与清理边界；机器性能、验证队列、租约、证明和警报均保留在本地状态。
- Codex 钩子对官方支持的本地工具路径实施写前拒绝，但不假称操作系统级强制；未覆盖路径以保留现场和脏状态警报兜住。

## 交付流程

```text
普通修改：Start → 隔离工作树 → 精确 Commit → Ready → 本地 Finish

当前环境：用户明确直改 → in-place Start → 当前工作树精确 Commit
          → 相对开始提交验证 → Finish（不移动、不清理）
```

直改进行时，其他隔离任务仍可开发；同一基线的 Finish 必须等待直改结束，随后同步新基线并按受影响范围重新验证。
