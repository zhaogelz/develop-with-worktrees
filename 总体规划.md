# 总体规划

## 产品定位

`develop-with-worktrees` 是技术栈无关的本地 Git 生命周期内核，用于让个人或小团队同时推进多个 AI 修改任务，而不让任务互相污染。核心 CLI 可被不同宿主适配；当前插件为 Codex 提供技能、钩子和默认工作方式。

## 固定原则

- 首次准备修改一个未选择过的仓库时，只用通俗语言问一次“三选一”；只读分析不问。
- 默认选择是独立目录。它从当前本地分支派生，任务完成后只在本地合回；用户不需要手工维护工作树，也不固定 `main`。
- “本次直接改当前目录”让该 Codex 会话像未安装插件一样普通开发：不创建策略、不建任务、不走 Commit/Ready/Finish，也不把普通写入记成保护告警。
- “以后都直接改当前目录”只写当前机器/克隆的 Git 本地状态，不提交、不影响他人；重新选择独立目录即可恢复。
- Codex 没有可靠的父子智能体钩子标识。需要写入的子智能体只能通过一次性显式委托继承本次选择，绝不因同仓库或时间窗口而放行无关并行任务。
- 高级 `in-place` 生命周期为明确要求保留精确提交、验证和 Finish 保护的兼容场景保留，不等同于“本次直接改当前目录”。
- 仓库只声明验证与清理边界；机器性能、验证队列、租约、证明、偏好和临时会话授权均保留在本地状态。
- Codex 钩子对官方支持的本地工具路径实施写前拒绝，但不假称操作系统级强制；未覆盖路径以保留现场和脏状态警报兜住。

## 交付流程

```text
首次修改：三选一
  选独立目录 → Start → 隔离目录 → 精确 Commit → Ready → 本地 Finish
  选本次当前目录 → 当前会话普通开发
  选长期当前目录 → 此仓库本机普通开发
```

独立任务仍可并行开发；高级直改任务进行时，同一基线的 Finish 必须等待直改结束，随后同步新基线并按受影响范围重新验证。
