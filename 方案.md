# 唯一实施方案：0.2.0-beta.5 成熟工作流绝对优先的轻量路由

1. 新增纯标准库 `solo_ai.routing`，集中维护成熟工作流标记和唯一优先级：`defer → disabled → current-task → managed → ask`。
2. 新增只读 `dww route --json [--session <id>]`；CLI、状态报告和生命周期 mode 均调用共享路由，不再自行拼装优先级。
3. Codex Hook 复用共享路由：`SessionStart` 为 `defer` 与 `current-task` 注入一次短上下文；成熟工作流的 Pre/Post Tool Hook 直接返回，不重复注入。
4. `choose` 在任何 mode 前先路由；命中成熟工作流时返回 `deferred`，不写偏好、会话授权、策略、任务或槽位。
5. 技能先消费 SessionStart 路由；上下文缺失时只执行一次 `route`。仅 `ask` 展示原三选一；`defer` 遵循仓库自身指令，`managed` 启动隔离任务，`disabled/current-task` 普通当前目录开发。
6. 更新总体规划、当前需求、README、架构、配置/生命周期/安全参考和插件提示，删除“本机偏好优先于成熟工作流”的旧口径。
7. 用纯路由单测、Hook 单测、生命周期集成测试和 CLI 集成测试覆盖成熟工作流叠加选项 2/3、SessionStart 一次注入、Pre/Post 静默让路、`choose` 零写入和未知仓库继续三选一；完成全量验证后发布 `0.2.0-beta.5`。
