# 唯一实施方案：0.3.0-beta.4 多 AI 指挥中心

1. 新增独立 `solo_ai.orchestration` 包：计划模型、前沿调度、状态库与生命周期适配器接口；不改写 `StateStore` 或 DWW 任务状态。
2. 在 Git common-dir 新建 `solo-ai-orchestration/batches`、`receipts` 与 `locks`。批次在 `awaiting-confirmation` 状态仅记录计划；确认后才进入 `running`。
3. 前沿调度以完成依赖、开发上限和 DWW 空闲槽位为条件；忽略普通 `write_scope` 重叠，只对显式 `exclusive_resources` 串行。
4. 中央会话为任务 claim 一名写入者并关联 DWW 任务引用。worker 通过既有 `start → commit → ready → finish` 完成自己的生命周期；编排层只保存引用与验收证据。
5. 编排层支持暂停、恢复、会话接手、取消保留代码、局部阻塞、有效修复尝试与批次紧凑回执。新内部任务必须显式声明仍在已批准目标内。
6. 生命周期适配器分为 `dww` 与 `delegated`：前者只在 `managed` 仓库可用，后者只记录明确外部生命周期引用，不执行或猜测外部命令。
7. CLI 新增 `dww orchestrate` 命令族；`--json` 使用 ASCII 安全转义，使非 UTF-8 控制台也能稳定传输 Unicode 数据；Codex 技能把复杂任务的计划确认作为默认隔离选择的唯一确认，并要求总控独占派发权限。
8. 用单元与 CLI 集成测试覆盖确认前不派发、DAG 前沿、最大 5、同文件乐观并行、风险独占、局部阻塞、修复升级、证据账本、取消保留、独立状态、Hook 命令识别、`cp1252` 下的 Unicode JSON 输出，以及 Windows 并发委托读取锁所有者时的瞬时竞争重试；最后跑全量测试与手工设计复审。
9. DWW 始终只完成本地 Finish。用户明确要求同步远端时，从干净基线工作区先执行当前分支的普通 push dry-run，通过后才非强制推送；远端分叉即停止，不自动 pull、rebase 或强推。
10. 固定 `hooks/hooks.json` 的定义哈希作为信任兼容契约；普通版本更新只替换实现，不触发重复信任。仅 Codex 明确报告待审时由 AI 解释变化并请求一次确认，获准后优先使用宿主界面控制完成 `/hooks`；不修改信任存储、不使用跳过信任参数，也不伪装成企业 managed hook。
11. 验证模块接受可选的预期基线，并在每个 profile 取得机器验证名额后、每条命令执行前核对；Ready 在验证后再次核对基线，发现并发推进时在同一次调用内同步并重试。命令失败时若基线已变化则丢弃旧结论并收敛，基线未变则继续硬失败。输入未变的 profile 证明按既有内容寻址规则复用，自动收敛最多 5 次；候选变化、基线回退、冲突和 Finish 最终一致性继续硬失败。
