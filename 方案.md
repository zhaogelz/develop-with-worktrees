# 第一阶段唯一实施方案

## 1. 总体策略

在现有生命周期上增量改造。保留采用、槽位、精确提交、候选证明、FIFO 集成和安全卸载的已有能力；用新增测试固定行为后，分别替换验证执行、验证计划、基线分支和清理所有权模块。

## 2. 状态与运行记录

继续以 `<git-common-dir>/solo-ai/state.json` 作为当前状态看板，不引入数据库。

新增本地、未跟踪记录：

```text
solo-ai/
├── operations/<operation-id>.json
├── validation-runs/<run-id>.json
├── integration-receipts/<task-id>.json
├── cleanup-plans/<plan-id>.json
└── ownership/<slot-id>.json
```

- 操作开始即写入 operation 小票，记录任务、脱敏进程身份、心跳和关联运行记录。
- 验证子进程启动后写入 validation run；终止状态只可为 natural、graceful、forced、timed_out、interrupted 或 ownership_unknown。
- 首次基线合入成功后写 integration receipt，后续收尾必须幂等。
- 插件首次创建槽位时写 ownership 记录；裁剪、卸载和删除只接受所有权匹配的路径。

## 3. 配置演进

保留 `config.toml` schema 2 的兼容读取；验证策略新增 schema 3，同时读取 schema 2。

```toml
schema_version = 3
static_only = false

[[profiles]]
id = "backend-unit"
level = "ready"
paths = ["backend/**"]
input_paths = ["backend/**", "pyproject.toml", "uv.lock"]
input_closure = "complete"
external_state = "none"
cross_task_reuse = false
environment = ["PYTHONUTF8"]
timeout_seconds = 1200
resource_class = "normal"
commands = [["uv", "run", "pytest", "backend/tests"]]
```

- `ready` 为日常必要闭包；`full` 仅显式验证。
- 只有 `input_closure = "complete"`、`external_state = "none"` 且输入摘要一致时，profile 才可跨候选复用。
- `cross_task_reuse = true` 仍需额外显式启用。
- 命令必须是 argv；扫描阶段禁止导入或执行仓库扩展代码。

## 4. 验证执行与证明

将当前验证逻辑拆为三步：

```text
build_validation_plan
    → 路径映射、未映射文件、层级、预计耗时、复用原因
run_profile_plan
    → 受控环境、运行记录、超时、日志、profile 证据
assemble_candidate_proof
    → 任务候选、基线、实际 profile 证据的精确证明
```

- `plan` 只读展示选择原因、预计耗时与未映射路径。
- `verify` 仅运行已登记 profile，供开发期产生证据。
- `ready` 选择 ready profile、阻断未映射路径并组装候选证明。
- `verify --level full` 显式运行 ready 与 full profile，不成为日常 Finish 前置。
- 受控执行仅传入基础系统变量和 profile 声明变量；值仅参与哈希。

## 5. 基线分支

- `start --base <branch>` 可显式指定基线；未指定时使用调用工作树当前本地分支。
- 任务保存 `base_ref`、`base_head` 和 `base_worktree`。
- `_sync_base` 只允许基线相对 `base_head` 正常前进；同步前使用只读预测，冲突保留任务现场。
- 基线删除、回退或非祖先关系必须由 `retarget` 经用户确认处理，并使现有证明失效。
- Finish 在记录的干净基线工作树中 `ff-only` 合入，不假定该分支为 `main`。

## 6. 清理所有权

- `workspace.toml` 在后续阶段承载复杂路径声明；第一阶段先在 `config.toml` 采用精确 `cleanup.owned_paths` 与 `cleanup.protected_paths`。
- 路径解析必须位于登记槽位内，拒绝链接、junction、路径替换和计划变化。
- `prune-slot` 先生成计划，确认时传入计划 ID 与摘要；实际执行重新计算并比较。
- Finish 默认保留依赖和缓存；Abandon 对普通未追踪文件先展示将丢弃的清单。

## 7. 实施顺序

1. 添加规划、需求和方案文档，建立针对性失败测试。
2. 实现运行小票、可超时验证执行器和 Recover 子进程检查。
3. 实现验证计划、逐文件未映射门禁、profile 层级和输入闭包复用。
4. 实现开发期 verify、全量 verify、耗时记录和 plan。
5. 实现基线分支、retarget 和 Finish 集成收据。
6. 实现清理所有权、两阶段 prune 与动态槽位。
7. 运行完整测试、跨平台 CI、仿真仓库端到端验收；根据审查结果循环修正。

## 8. 当前实施进度

- 已完成第 1 步；规划、需求和唯一方案已进入版本库。
- 已完成第 2 步的验证运行器：每条验证命令具备硬超时、30 秒默认心跳、脱敏日志、进程身份快照、受控环境和本地运行回执；仅在根进程身份仍匹配时终止其进程树。
- 已完成第 3 步的第一部分：schema 3 可兼容读取 schema 2，Ready 会阻断未映射改动；同一任务仅在声明输入未变时可复用，跨任务复用要求完整输入闭包和无外部状态。
- 已完成第 5 步的基线主链：Start 默认以调用工作树当前本地分支为基线，也可显式 `--base`；任务记录基线工作树，Finish 只合入记录的干净基线；基线删除、回退或改写会阻断，必须在用户解决差异后以精确确认的 `retarget` 重绑。
- 后续继续实施开发期验证命令、集成收据、清理所有权和动态槽位，并以端到端测试作为每批的合格门槛。
