# 唯一实施方案：0.2.0-beta.4 一次三选一、默认隔离

1. 新增 `dww choose --mode isolated|current-task|current-repository` 作为首次选择的唯一执行入口；保留旧 `init/disable/enable` 只作高级兼容接口。
2. `isolated` 自动复用 `initialize`，并在无发现测试时内部接受 static-only；`current-repository` 复用 `disable()` 的活动任务、队列和锁检查；用户明确重新选择隔离时先安全地重新启用本机偏好。
3. `current-task` 在 Git common-dir 的 `solo-ai/session-overrides.json` 写入精确工作树、会话哈希和委托码哈希，不写追踪文件、不建 DWW 状态。父会话得到一次性委托码；子会话显式登记该码后才加入；当前目录已有活动 DWW 任务时拒绝退出，防止遗留失控状态。
4. Hook 在长期本机退出后或有效任务级会话授权后立即退出。未选择仓库只放行受信 `dww choose`，其他潜在写入拒绝并要求先完成一次三选一；选项 2 的 PostToolUse 也不得写脏基线警报。
5. 技能、README、架构和参考文档只呈现通俗三选一；测试发现、静态验证、委托和高级直改保留为内部/进阶说明，不制造第二个用户选择。
6. 用生命周期、CLI、Hook 单元测试覆盖三种选择、会话隔离、子会话显式委托、无原始秘密落盘、PostToolUse 无告警、长期退出的在途任务拒绝与重新启用；完成全量测试后发布 `v0.2.0-beta.4`。
