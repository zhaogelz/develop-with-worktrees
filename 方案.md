# 唯一实施方案：0.2.0-beta.3 默认隔离、一次受控直改

1. 保持 beta.1 的隔离槽位、精确提交、路径验证、验证队列和本地合并作为默认路径。
2. 本地状态升级到 schema 3：旧 schema 2 任务读入时补为 `isolated`；新增 `in-place` 任务，不占槽位，保存 `start_head`、`expected_head`、基线工作树/分支、会话指纹与租约。
3. `start --in-place --session` 只在当前干净、附着分支的工作树登记该任务。普通任务仍可从其最新已提交 HEAD 派生。
4. 直改的 Commit、Ready、Verify、Finish 都校验会话/工作树/分支/HEAD；验证固定相对 `start_head`，且强制任务内证明范围。Finish 只生成完成小票并释放状态。
5. 当同一基线存在活动直改任务，隔离任务 Finish 明确等待，不自动合并或语义解决冲突。
6. Codex hook 改为官方 PreToolUse 硬拒绝协议：基线工作树默认 fail-closed，严格只读 Bash 和 DWW 生命周期命令白名单通过；有效直改租约才放行写入。PostToolUse 仅发现并记录逃逸脏改动。
7. 验证超时时，直接使用当前 `Popen` 持有的进程组终止权；不再用仅适用于跨调用恢复的身份快照复核该同一进程，避免 macOS 元数据差异导致超时命令自然跑完。
8. 更新 CLI、技能、参考文档、README、架构、版本和测试；完成多层测试、真实临时仓库生命周期与插件安装检查后，以新 `v0.2.0-beta.3` 发布。
