# 唯一实施方案：0.2.0-beta.1 干净换代

1. 仅迁移本仓库的验证策略到 schema 3；发布产物拒绝 schema 2。
2. 在验证执行器外加入机器全局加权 FIFO 队列；容量由稳定硬件信息计算，`settings` 只写机器本地设置。
3. 记录成功 profile 的本机时长中位数，`plan` 输出估算和大于十分钟的建议。
4. 将清理默认改为零候选；`prune-slot` 对声明目标做计划、内容摘要、复核和删除，任何 `.env*`、链接/junction 或变化都停止。
5. 提供 `version` 与候选策略 `approve --task`，把版本与安装路径纳入自动测试。
6. 更新技能、参考文档、README、变更日志和验收测试，只描述这一条方案。
7. 完成源码测试、临时安装成品端到端验证、当前仓库 schema 3 smoke 后，安装 beta.1 并精确删除本机 beta.2 缓存。
