# 当前有效需求与验收约束

## 目标

完成 `0.2.0-beta.1` 的干净换代：只保留 schema 3 验证策略，不保留旧 beta.2 的运行时兼容；新版本必须既方便日常快速开发，也不在清理、基线或验证中静默冒险。

## 必须满足

1. `start` 默认从当前本地分支派生，Finish 仅合回记录基线；团队继续本地合并后自行提交 PR。
2. 验证 profile 支持 development/ready/full；未映射的 Ready 改动必须阻断，full 只能显式执行。
3. 机器全局按加权 FIFO 排队：normal 占一个容量，heavy 独占计算容量；默认自动计算，用户可本机调整，仓库不能抬高限制。
4. 保存本机成功时长中位数；预计超过十分钟时建议拆分或减少重复准备，不自动削减覆盖。
5. Finish 不自动清理。`prune-slot` 仅处理精确 `cleanup.owned_paths`，计划必须给出路径、理由、大小、摘要；目标内 `.env*`、链接/junction 或变化均停止整次删除。
6. 版本、插件清单、Python 元数据和 CLI 输出一致；必须验证源码、安装成品和真实临时仓库生命周期。
7. 本地安装验证成功后，移除本机 beta.2 缓存；不公开发布、不自动更新其他人的机器。

## 不做

- 不扫描或迁移其他项目，不提供通用旧策略迁移命令。
- 不添加 `workspace.toml`。
- 不自动 push、PR、远端同步或删除用户数据。
