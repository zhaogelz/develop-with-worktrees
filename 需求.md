# 当前有效需求与验收约束

## 目标

交付第一版多 AI 指挥中心：在保留现有 DWW 单任务生命周期的前提下，让 Codex 中央会话能为复杂目标生成一次确认的计划、调度最多 5 个独立任务、保留可恢复的本地编排状态，并按证据完成验收。

## 必须满足

1. 简单任务沿用单 AI 生命周期；复杂任务在用户一次确认前不启动 worker 或底层任务。
2. 编排计划使用纵向、用户可独立验收的任务；支持依赖与合同优先任务。
3. 实际开发并发不超过 5，也不超过仓库 DWW 空闲槽位；验证复用现有加权队列。
4. 同一文件默认允许乐观并行；只有显式 `exclusive_resources` 才串行。每个任务只有一个已记录写入者。
5. 被阻塞任务只阻塞其下游；无关任务仍能进入前沿。重复两次无变化失败必须升级；有变化修复受默认 3 次有效变更或 20 分钟、且可配置的预算约束。
6. 编排状态与 DWW 的 `solo-ai` 状态严格隔离，存于 Git common-dir 的 `solo-ai-orchestration`；不得保存完整聊天、原始推理、lease、密钥或项目内任务 Markdown。
7. 支持暂停、恢复、取消保留代码、新中央会话接手；取消和接手均需要精确的内部确认值，且不删除分支或文件。
8. 每个完成任务都必须登记既有 Ready/Finish 证明或等价引用；批次只在全部任务完成且证据齐全时结束，并产生紧凑回执。不默认跑全仓库 Full。
9. 普通仓库的 `dww` 适配器只允许 `managed` 路由；成熟仓库必须显式用 `delegated` 适配器，编排器不猜测、不执行外部生命周期。
10. 中央 Codex 会话是唯一支持的派发入口；worker 提示不允许再派发。该约束是宿主工作流约束，不宣称操作系统级强制。
11. DWW 与编排器不自动触及远端：不 fetch、pull、push、PR、部署、发布、rebase、squash、amend 或改写历史。Finish 成功后，用户明确要求的普通非强制 push 属于独立发布步骤，不扩大 DWW 生命周期权限。

## 不做

- 不实现常驻后台、默认 reviewer、多服务运行时合同或通用 proof DAG。
- 不自动解决文本/语义冲突，不把失败任务无变化地重复运行。
- 不把任意成熟仓库标记误当作可由 DWW 接管的普通仓库。
