# 当前有效需求与验收约束

## 目标

完成 `0.2.0-beta.5`：修复已有成熟工作流的仓库仍触发 DWW 三选一、且选项 2/3 会压过仓库自有流程的问题；保持首次选择简单、路由低延迟、低 Token、零额外项目写入。

## 必须满足

1. 仓库存在已登记成熟工作流时，路由结果必须为 `defer`；它高于长期当前目录偏好、当前会话授权和已采用 DWW 策略。
2. `defer` 不展示三选一、不初始化 DWW、不创建槽位，不新增、删除或改写 Git common-dir 下的 DWW 偏好和会话授权。
3. 已存在的选项 2/3 状态在成熟工作流存在期间只被忽略，不自动清理；成熟工作流标记移除后，原 DWW 偏好可重新生效。
4. 新增只读 `dww route --json`，只返回 `defer|disabled|current-task|managed|ask` 及极小原因信息；不得加载完整诊断报告。
5. 工作流标记与路由优先级只能维护一份纯标准库实现，CLI 与 Codex Hook 必须复用，不能各自复制判断。
6. Hook 已信任时，`SessionStart` 只注入一次短路由上下文；`defer` 的 Pre/Post Tool Hook 直接让路，不重复消耗上下文。
7. Hook 上下文不可用时，技能在首次修改意图前只调用一次 `route`；只有结果为 `ask` 时才显示既有三选一。
8. 对成熟工作流仓库误调用 `choose` 时，三种 mode 均返回 `deferred` 且零 DWW 写入。
9. 没有成熟工作流的仓库继续保持 beta.4 的三选一、会话绑定、子会话显式委托、默认隔离、Commit/Ready/Finish 和安全边界。
10. 版本、插件清单、Python 元数据、CLI、技能、Hook、文档和自动测试一致，以 `0.2.0-beta.5` 发布。

## 不做

- 不解析任意自然语言 `AGENTS.md` 来猜测工作流；只使用明确、可测试的标记清单。
- 不自动迁移、删除或改写仓库已有工作流。
- 不自动清理用户已经选择的本地偏好或会话授权。
- 不把完整 `doctor` 作为每次任务的路由入口。
- 不改变远端、历史改写和数据清理边界。
