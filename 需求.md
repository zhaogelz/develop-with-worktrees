# 当前有效需求与验收约束

## 1. 当前目标

完成第一阶段安全闭环改造，使插件可以在不牺牲日常快速开发体验的前提下，可靠地处理验证中断、基线分支并行、验证范围选择和本地清理。

## 2. 本阶段必须实现

### 验证可靠性

- 每个已登记验证命令有可配置硬超时，默认 2700 秒。
- 无输出长命令仍能按时检查超时，并至少每 30 秒写入心跳。
- 验证子进程以身份快照、运行记录和脱敏日志绑定任务。
- 超时只停止身份匹配的本任务进程树；无法确认所有权时保留现场。
- 调用者中断但验证子进程仍存活时 Recover 必须拒绝；子进程已退出时可显式 Recover 并保留诊断记录。

### 验证选择与证明

- profile 支持 `ready` 与 `full` 层级；日常 Ready 不执行 `full`。
- 开发期只能执行已登记 profile，并可产生 profile 证据。
- Ready 必须逐文件检查映射；任何未映射活跃路径阻止 Ready 并输出建议。
- 同任务中，声明完整输入闭包且无外部状态的 profile 可按输入摘要复用；候选证明仍精确绑定候选和基线。
- 命令、输入、锁文件、工具、声明环境、策略或日志变化时，使对应证据失效。
- 证明、日志和状态不得保存环境变量原值、lease 或未脱敏命令行。

### 基线分支与集成

- Start 默认从调用位置当前检出的本地分支派生；detached HEAD 必须显式给出基线。
- 任务记录基线分支、基线提交和基线工作树；不得从活动受管任务分支派生。
- Ready/Finish 只同步和集成记录的基线分支。
- 基线正常前进时允许同步；删除、回退或历史改写时停止并要求显式 retarget。
- Finish 合并后先写可恢复的集成收据；崩溃后能够幂等完成 detach、分支清理和状态释放。

### 清理所有权

- 默认不因目录名自动信任或删除缓存、依赖、`uv.toml` 等文件。
- 只有插件登记或仓库精确声明的路径才可成为清理候选；受保护和未知内容阻止清理。
- `prune-slot` 先创建带路径、理由、大小和摘要的计划，再以计划摘要确认；执行前变化即拒绝。
- Deinit 仅处理同时满足路径、Git 注册和插件所有权记录的槽位。

### 通用性与易用性

- 槽位默认 3 个，支持配置到最多 32 个；缩容使用 draining 语义。
- 默认遵守仓库换行规则；仅在检测到实际问题且仓库声明后调整本地 Git 设置。
- 计划批准只在验证、安全或执行边界发生实质变化时重新要求。

## 3. 本阶段非目标

- 不迁移或接管 `亿发姆 x` 的现有工作树流程。
- 不自动 push、创建 PR、pull、rebase、squash 或改写历史。
- 不内置 PostgreSQL、Docker、特定业务运行时或完整系统级沙箱。
- 不实现完整的长期 schema 迁移体系。
- 不实现复杂验证 DAG、多服务编排和依赖指纹的完整高级能力。
- 不为缩短耗时自动降低必要验证覆盖。

## 4. 验收条件

- Python/uv 与 Node/npm 普通仓库可完成低配置生命周期。
- 三个并发 Start 不得领取同一槽位；同一基线的 Finish 保持 FIFO。
- 验证超时、调用者中断、基线前进、基线回退、合并冲突、未映射文件和未知清理对象均有自动化测试。
- 简单仓库和复杂 Monorepo 仿真仓库各完成一次端到端生命周期测试。
- Windows、Linux、macOS CI 通过；真实 Codex 插件安装、升级、停用和卸载验证通过。
- 每次改动后执行对应定向测试；完成前运行完整测试、静态检查和格式检查。
