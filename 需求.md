# 当前有效需求与验收约束

## 目标

完成 `0.2.0-beta.4`：将未选择仓库的复杂初始化和测试发现收敛为一次人人能懂的三选一，并让默认隔离开发继续保持安全、方便和技术栈无关。

## 必须满足

1. 只读分析不询问；首次准备修改未选择仓库时只展示一次约定的“三选一”文案，不出现初始化计划、静态模式、测试发现或工作树术语，不再二次确认。
2. 选“每个任务使用独立目录”后，复用默认隔离生命周期；未发现自动测试时内部使用基础检查，不阻塞用户选择。
3. 选“这一次直接改当前目录”后，当前会话在精确当前工作树内完全跳过本插件：不创建 `.solo-ai`、`AGENTS.md` 管理块、任务、槽位或验证证明，也不要求 DWW Commit/Ready/Finish，Hook 的前后置检查均不拦截或记录普通写入。
   当前目录已有活动 DWW 任务时必须先 Finish 或放弃，不能用本次退出把它遗留为失控状态。
4. 本次选择必须绑定会话和当前工作树；新会话不匹配即重新走正常选择。子智能体仅凭父任务的一次性显式委托码加入，不能用仓库级或时间窗放行无关任务；本地状态不保存原始会话标识或委托码。
5. 选“以后都直接改当前目录”后，只写 Git common-dir 的本地偏好、未来不再询问；已有活动/隔离任务、队列票据或生命周期锁存在时必须拒绝，不得搁置清理。
6. 用户以后用自然语言改为独立目录或长期当前目录时可直接改变本机选择；不同机器、不同克隆独立。
7. 保留 `start --in-place` 的高级兼容语义和既有生命周期安全边界，但不把普通“本次当前目录”映射为它。
8. 版本、插件清单、Python 元数据、CLI、文档和自动测试一致；以 beta.4 新版本发布，不能覆盖 beta.3。

## 不做

- 不自动 push、PR、远端同步、rebase、squash、amend、改写历史或删除用户数据。
- 不要求用户手工维护工作树或写 `workspace.toml`。
- 不假称 Codex 已提供自动父子智能体关联；没有宿主标识时不用猜测、时间窗或仓库级旁路代替。
