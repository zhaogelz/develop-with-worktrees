# 当前有效需求与验收约束

## 目标

完成 `0.2.0-beta.2`：让大多数 AI 改动默认方便地隔离执行，同时为确实依赖当前测试/数据环境的任务提供一次安全、可理解、不会悄悄破坏并行开发的直改通道。

## 必须满足

1. 普通 `start` 从当前本地分支创建独立任务工作树；任务精确提交、按 schema 3 路径验证，并仅在本地合回记录基线。
2. `start --in-place` 只允许干净的当前附着分支和明确 Codex 会话；被忽略的测试数据可存在，但 tracked/nonignored 脏改动必须先保留处理。
3. 直改任务不占槽位、不建分支，记录不可变 `start_head`、受控 `expected_head`、分支、工作树和会话指纹。每次直改提交只能通过精确路径 `dww commit` 推进 `expected_head`。
4. 直改的 plan、verify、Ready、Finish 都相对 `start_head`；直改证明不可跨任务复用。Finish 后保持当前分支和被忽略数据，不执行 merge、detach、reset、clean、删分支或删缓存。
5. 会话、工作树、分支或 HEAD 漂移会隔离任务并保留所有文件；普通 recover 不能绕过。前一 Codex 任务结束后，用户可精确确认 `resume-in-place` 接管未变化的 active/ready/quarantined 任务；发生身份漂移仍必须先手动恢复原身份。
6. 直改期间，隔离任务允许从最新已提交基线 Start；同基线 Finish 必须阻止，待直改结束后再同步、重新验证并处理真实冲突。
7. Codex trusted hook 对 Bash、apply_patch、Edit/Write 等支持路径在写前返回标准 `permissionDecision: deny`；严格只读和 DWW 生命周期命令可通过。它必须说明信任前不可依赖，且不承诺操作系统级绝对防护。
8. 未授权脏基线和钩子未覆盖的事后写入只记录本地警报、保留现场并停止后续自动写入，绝不自动回滚、搬运或清理。
9. 版本、插件清单、Python 元数据、CLI、文档和自动测试一致；发布 beta.2 为新版本，不能覆盖 beta.1。

## 不做

- 不自动 push、PR、远端同步、rebase、squash、amend、改写历史或删除用户数据。
- 不要求用户手工维护工作树或写 `workspace.toml`。
- 不把 Codex 适配误称为唯一可用宿主；其他宿主可另做适配器。
